-- ============================================================
-- SNAPSHOT احترازي — قبل تنظيف الطلبات الفاشلة
-- التاريخ: 2026-05-30
-- المُنفِّذ: PM Ahmed (Senior 16y) عبر Lovable
-- السبب: إكمال نية الطلب الأصلي «حذف الملغاة والتي فشل الدفع»
--        الطلب الفاشل LG-260530-7801 لم يُحذف سابقاً بسبب
--        عدم تطابق المُسمّيات (payment_failed مقابل failed).
-- ============================================================

-- توزيع الحالات وقت أخذ الـ snapshot:
--   fulfilled      = 6  (المجموع 389.00)  | test=0
--   paid           = 4  (المجموع 419.00)  | test=0
--   payment_failed = 1  (المجموع 400.00)  | test=0
--   cancelled      = 0  (حُذفت في عملية سابقة)
--   failed         = 0  (غير مستخدمة للطلبات إطلاقاً)

-- ------------------------------------------------------------
-- السجلّ المرشَّح للحذف (intent: failed-payment cleanup)
-- ------------------------------------------------------------
-- ORDER:
--   id            = fda66b34-df0a-49ad-b698-6bd7f17b03a5
--   order_number  = LG-260530-7801
--   status        = payment_failed
--   is_test       = false
--   total         = 400.00 SAR
--   customer_name = ابو ناصر
--   customer_email= abu-naser56@gmail.com
--   created_at    = 2026-05-30 02:54:45.014246+00
--   product       = bundle-falcon-hulk-1y (سنة كاملة)
--
-- PAYMENT_TRANSACTION المرتبطة:
--   id               = ae412dce-5d25-4aac-b127-c6260568acff
--   status           = initiated  (لم يكتمل الدفع)
--   amount           = 400 SAR
--   provider         = edfapay
--   provider_order_id= a385f20a-5053-4ec7-a901-43cd729470b5
--
-- التبعيات (تم التحقق — كلها صفر، حذف آمن):
--   payment_fees       = 0
--   refunds            = 0
--   subscription claimed= 0

-- ------------------------------------------------------------
-- صف الاستعادة (rollback reference) — للتوثيق فقط
-- ------------------------------------------------------------
-- لإعادة إنشاء الطلب يدوياً عند الحاجة، القيم الأساسية أعلاه.
-- لا يمكن استعادة الحذف تلقائياً؛ الحذف نهائي.

-- ============================================================
-- نهاية الـ snapshot
-- ============================================================
